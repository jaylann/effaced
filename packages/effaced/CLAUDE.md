# effaced (core package)

Storage-agnostic core. **No module outside `adapters/` may import SQLAlchemy or any storage library.**

## Module map (one concept per file; file = class)

| Package | Holds | Key invariant |
|---|---|---|
| `categories/` | `PiiCategory`, `LegalBasis`, `ErasureStrategy` enums | members are manifest format — removal/rename = MAJOR |
| `annotations/` | `PiiSpec`, `RetentionPolicy`, `SubjectLink`, `SubjectRef` (frozen pydantic) | `RETAIN` requires a `RetentionPolicy` (validator) |
| `manifest/` | `DataMap`, `TableEntry`, `ColumnEntry`, `migration.py` | format change ⇒ bump `MANIFEST_SCHEMA_VERSION` + migration branch; old payloads never rejected |
| `manifest/resolution/` | `JoinHop`, `TableAccessPlan`, `SubjectGraph`, `fk_safe_deletion_order()` | pure data + stdlib graphlib, runtime-only (never serialized); incoherent graphs are unrepresentable |
| `lint/` | `CompletenessFinding` (frozen pydantic; `column=None` ⇒ whole table unannotated) | findings are questions, not verdicts — never a compliance determination; storage-agnostic, linters live in adapters |
| `adapters/sqlalchemy/` | `pii()`/`subject_link()` info-dict helpers, `collect_data_map()`, `resolve_subject_graph()`, `lint_completeness(metadata)` (exact complement of the collector; PK/FK + `effaced_*` exempt), `SurrogateRegistry`/`default_surrogate_registry()`, `ErasureExecutor(metadata, surrogates)` (the `StepExecutor` impl: hop-chain-scoped DELETE/per-row ANONYMIZE/RETAIN count), `RectificationExecutor(metadata)` (the `RectificationStepExecutor` impl: one scoped UPDATE per step, one shared value per cell — deliberate, unlike anonymize's per-row surrogates), `ErasureVerifier(data_map, graph, metadata, *, audit_sink)` (read-only post-erasure read-back: re-derives the ADR 0007 classification via an internal `ErasurePlanner(...).plan()` — never re-implemented — and SELECT-counts per table through `subject_scope`; `verified` ⇔ every row-deleted table empty, surviving anonymize/retain counts informational; one `ERASURE_VERIFIED`/`ERASURE_VERIFICATION_FAILED` scalar-only event; proves execution fidelity only — same annotated surface, off-path orphans + anonymized values out of scope; issue #121), `scoping.py` (the shared hop-chain predicate: `lookup_table`/`subject_scope`/`coerce_subject_id`/`grouped` — both executors and the verifier use it, never fork it), `storage/` (`bind_tables()` → `EffacedTables`: the effaced-owned `effaced_*` tables) | the ONLY SQLAlchemy-aware code; `collect_data_map()` is also the retention-anchor validation site (anchor must exist on the table and be `DateTime`-typed, else `ManifestError` — ADR 0012) |
| `export/` | `Exporter`, `ExportBundle`, `ExportRecord` | failures land in `incomplete_sources`, never silent omission |
| `erasure/` | `ErasurePlanner(data_map, graph, registry, *, executor, outbox, audit_sink)`, `ErasurePlan`/`ErasureStep`, `ErasureResult`, `ErasureVerification` (frozen pydantic post-erasure verdict; `verified` ⇔ all `residual` counts zero, validator-pinned; `ErasureVerifier` lives in `adapters/sqlalchemy/`), `StepExecutor` protocol | plans are inspectable before execution; local steps + outbox enqueue atomic in the caller's session (never committed here); row-delete vs anonymize-in-place is ADR 0007, ref→resolver routing is ADR 0008 (kind == name; unmatched kind ⇒ `ResolverError`, unmatched resolver ⇒ skipped + audited), execution/audit/re-run semantics are ADR 0009 — changing any is MAJOR |
| `consent/` | `ConsentLedger(consent_records, audit_sink)`, `ConsentRecord` | records are immutable events; status is derived (latest per subject+purpose; timestamp ties resolve to withdrawn), never stored; rows write via the caller's session, every `record()` mirrors an event into the constructor's `AuditSink` |
| `restriction/` | `RestrictionLedger(restriction_records, audit_sink)`, `RestrictionRecord` (`purpose=None` = all processing) | flag + record, never enforcement (no engine consults it; ADR 0014); status is derived (latest global OR latest purpose; ties resolve to restricted; a purpose lift never undoes a global restriction; `status(purpose=None)` reads only global records), never stored; rows write via the caller's session, every `record()` mirrors a scope-only event (never `reason`/`source`) into the constructor's `AuditSink` |
| `retention/` | `RetentionSweeper(data_map, graph, metadata, audit_sink)`, `RetentionReport`/`RetentionReportEntry` | report-only sweep (ADR 0012): SELECT statements only, never mutates; sweepable iff policy has `duration` + `anchor`, everything else counted indeterminate (never guessed); eligibility ignores erasure strategy; one `RETENTION_EXPIRED` event per subject with expired rows (names + counts, never values/timestamps); the planner stays time-free — a delete mode is a future MAJOR ADR |
| `replay/` | `Replayer(planner, audit_sink, *, refs_for=None)`, `ReplayPlan(+Entry)` (`ReplayPlan.derive(events, backup_taken_at=...)` is the pure classification), `ReplaySource` capability protocol (`read_since`) | backup replay (ADR 0018): a restore resurrects locally-erased subjects; derive from a SURVIVING trail (the restored DB's own trail lost the window), trigger is `ERASURE_LOCAL_COMPLETED` with an inclusive boundary (`>= backup_taken_at`); interrupted/failed-only attempts are surfaced, never executed or guessed; `replay` appends `ERASURE_REPLAYED` append-first per subject then delegates to `erase_subject` (no second engine), fail-fast in the caller's session; refs re-derive via `refs_for` from restored data, default local-only; `ReplaySource` is deliberately NOT an `AuditSink` method (capability-protocol pattern) — changing any is MAJOR |
| `audit/` | `AuditEvent(+Type)`, `AuditSink` protocol, `DatabaseAuditSink(session_factory, audit_events)` (+ concrete `read_since(since)` — the `ReplaySource` capability, ADR 0018) | append-only by construction; payloads are small scalars, never rich PII; the database sink commits each append in its own short transaction (ADR 0006); unreadable `event_type` on read ⇒ `AuditIntegrityError`, never skipped |
| `rectification/` | `Rectifier(data_map, graph, registry, *, executor, outbox, audit_sink)`, `RectificationStep` (value-free — plans never carry PII), `RectificationResult`, `RectificationStepExecutor` protocol (value passed separately) | corrections are category-keyed `Correction` models (in `annotations/` — the outbox carries them at runtime); erasure strategy NEVER gates rectification (RETAIN/ANONYMIZE columns rewritten too); validation (empty/duplicate categories, unmatched ref kind) raises before any event; non-rectifying resolvers are skipped + recorded; audit payloads never carry values (ADR 0013 — changing any is MAJOR) |
| `resolvers/` | `Resolver` protocol, `RectifyingResolver` capability sub-protocol (optional `rectify_subject`; call sites narrow with `isinstance`), `RetentionOnlyResolver` capability sub-protocol (`schedule_erasure` → `ResolverScheduledErasure {expires_at XOR already_absent}`; implementations' `erase_subject` raises — ADR 0018), `ResolverRegistry`, result models (`ResolverRectification.already_consistent` = the convergence analogue of `already_absent`), `registry_from_settings(specs, settings=None)` → `RegistryBuild(registry, outcomes)` building a registry from config (`ResolverSpec` declares keys+builder; `SpecOutcome` records each register/skip) | public API, additive-only; explicit registration, no discovery — `registry_from_settings` is all-or-nothing per spec (blank value = absent; partial required keys ⇒ `ConfigurationError` naming key NAMES never values; name mismatch ⇒ `ConfigurationError`; builder gets exactly the declared present keys), skips are recorded in `outcomes`, never silent |
| `saga/` | `Outbox(session_factory, outbox, *, status_counts_source=None, audit_sink=None)`, `StatusCountsSource` protocol (SQL-side `status_counts` aggregation; adapter impl `SqlStatusCountsSource`), `OutboxEntry(+Status, +Operation, +corrections)`, `BackoffPolicy`, `SagaRunner(registry, outbox, audit, *, max_attempts, batch_size, backoff)` | `enqueue` writes flattened entries through the CALLER's session (same transaction as the local phase), never commits; `entry_id` is the idempotency key; rectify entries carry corrections in the row's `payload` (real PII — cleared on every terminal transition, kept on FAILED for the retry); `claim_batch`/`mark_*`/`list_abandoned`/`status_counts`/`requeue` use the constructor's `session_factory`; the operator surface adds one supervised mutation — `requeue(entry_ids)` (ADR 0015): **erase-only** ABANDONED→PENDING with a fresh budget (`attempts=0`, `next_attempt_at=NULL`, `last_error=NULL`), append-first `ERASURE_REQUEUED` (prior_error class-name only) before the flip in one FOR-UPDATE-by-`entry_id` transaction, skip-tolerant + idempotent, needs the constructor's `audit_sink` (else `ConfigurationError`); an ABANDONED **rectify** entry is REFUSED with `ConfigurationError` before any append/flip — its corrections were cleared at abandonment (ADR 0013), so re-issue via the `Rectifier`, not requeue; abandonment is terminal under ADR 0010 *until an operator requeues (erase) or re-issues (rectify)*; one `next_attempt_at` gate is both crash lease and backoff schedule, attempts count claims, `ResolverError` ⇒ immediate abandon (incl. a rectify entry hitting a non-rectifying resolver); completion is per (subject, operation): `ERASURE_COMPLETED` needs every erase entry SUCCEEDED, `RECTIFICATION_COMPLETED` every rectify entry; an erase entry hitting a `RetentionOnlyResolver` calls `schedule_erasure` — a future horizon appends `ERASURE_EXPIRY_SCHEDULED` (`{target, external, expires_at, prior_attempts}`) then `mark_scheduled` parks it `SCHEDULED` with the horizon as the `next_attempt_at` gate and a fresh budget (`attempts=0`, the requeue precedent; resume clamped to ≥ one backoff step), and the parked entry blocks `ERASURE_COMPLETED` until a post-horizon claim verifies `already_absent=True` (step-succeeded carries `verified_expiry`, never `strategy: delete`) — a schedule is never a completion (ADR 0018) (ADR 0010 as amended by 0013/0015/0018 — changing any is MAJOR) |
| `testing/` | `ResolverConformanceSuite`, `InMemoryResolver`, `InMemoryRetentionOnlyResolver` (injectable clock; the ADR 0018 reference fake — schedule tombstones, post-horizon purge, horizon-stamped exports, raising `erase_subject`), `assert_data_map_complete(metadata, *, exempt_tables, exempt_columns)` (CI gate over `lint_completeness`; stale exemptions fail) | public API, additive-only; imports pytest at runtime by design (ADR 0011) — NEVER re-export from root `__init__.py`; its `_run` is the second sanctioned `asyncio.run` bridge; the suite's erase tests skip for `RetentionOnlyResolver`s (their scheduled section runs instead, gated on `isinstance` + the `make_expired_resolver` hook) |

`__init__.py` files: docstring + re-exports only. New public class ⇒ new file ⇒ re-export ⇒ add to root `__all__` (test_public_api guards it). Exception: `testing/` stays out of the root `__all__` so `import effaced` never pulls pytest.

## When implementing engine logic

- Keep signatures — they are the published API surface; changing them is breaking. The sync/async shape is settled by ADR 0006 (sync engine `def`s taking the caller's `Session`; async only on `Resolver` methods and `SagaRunner.run_once`).
- Every outcome (success, failure, retention skip, abandonment) emits an audit event.
- FK-safe ordering comes from metadata, never from user-supplied order.
- Add bleed/retention/idempotency property tests alongside (see `.claude/rules/testing.md`).
- `Select.with_only_columns()` recalculates the FROM list from the new columns plus later `.where()` criteria — selecting a hop-chain alias's subject-id column off `table.select()` yields the implicit join the retention sweeper needs, no `select()` import in core.

## Erasure-executor invariants (ADR 0007/0008 — learned the hard way)

- Hop-chain subqueries: alias every *inner* level, keep only the outermost as the raw `Table`, or self-referential hops auto-correlate and scope wrong.
- `ANONYMIZE` updates row-by-row with fresh surrogates per cell; one scoped UPDATE writes a single shared surrogate and breaks unique constraints.
- `subject_id` is published as `str` but subject PKs are often `Integer`; coerce via `column.type.python_type` — typed-parameter drivers (psycopg3) reject `integer = text`.
- Refs route to the resolver whose `name` equals the ref's `kind` (ADR 0008): unmatched ref kind ⇒ `ResolverError` before any audit event; resolver with no matching ref ⇒ skipped + recorded in `skipped_resolvers`, never an error.
- A table with **zero** annotated columns that is fully PII-owned (only PK/FK columns) is row-deleted by the planner — `all(...)` over an empty column set is vacuously true (`Order` in the test schema is the precedent). Link-only tables are erased, not skipped.

## Saga invariants (ADR 0010 — learned the hard way)

- `mark_succeeded` locks the subject's outbox rows `FOR UPDATE` ordered by `entry_id` *before* updating — identical lock order across runners prevents lock-order-inversion deadlock; claimers never wait (`SKIP LOCKED`) so no cycle exists.
- `attempts` increments at *claim* time, not failure time — a runner crashing before bookkeeping would otherwise never raise the counter and the entry would be re-claimed forever instead of converging to ABANDONED.

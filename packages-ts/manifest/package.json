{
  "name": "@effaced/manifest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TS spike: emit a JSON manifest accepted by Python effaced DataMap.from_payload. Emit + round-trip only; no erasure/export/resolution.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}

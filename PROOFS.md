# PROOFS — guarantee → evidence map

effaced ships *mechanisms*, never compliance *determinations*. What it can
ship is evidence that those mechanisms behave as documented: this file maps
every published guarantee to the tests that exercise it. The test suite is
the project's conformance evidence — if a guarantee has no row here, it is
not a guarantee.

All test paths are relative to `packages/effaced/tests/`. Tags:

- **[unit]** — example-based test on the shared annotated schema
- **[property]** — hypothesis property on the shared schema (drawn data shapes)
- **[generated]** — hypothesis property on *arbitrary generated schemas*
  (`schema_strategies.py` draws table trees, subject-link paths, per-column
  strategies, deep self-referential chains, and composite foreign-key hops,
  derived through the real `collect_data_map` + `resolve_subject_graph`
  path; `rejected_schema_strategies.py` draws the shapes effaced refuses).
  Generated schemas are local-database-only — no resolver legs;
  saga/external semantics are proven by the saga properties and the fault
  matrix below
- **[pg]** — Postgres integration test (`just test-pg`); locking and
  concurrency claims are only provable here, because SQLite silently drops
  `FOR UPDATE` / `SKIP LOCKED`

## Guarantees

| Guarantee | Defined by | Proven by |
|---|---|---|
| **No cross-subject bleed — export.** A subject's bundle contains that subject's data and nobody else's. | ADR 0007/0008; `.claude/rules/testing.md` | [generated] `test_generated_schema_export_properties.py::test_export_on_any_schema_never_bleeds` · [property] `test_exporter_properties.py::test_export_never_contains_other_subjects_rows` · [unit] `test_exporter.py::test_no_cross_subject_bleed` · [pg] `test_exporter_pg.py::test_export_round_trips_on_postgres` |
| **No cross-subject bleed — erasure.** Erasing subject A leaves subject B's rows byte-identical, in every table. | ADR 0007/0009 | [generated] `test_generated_schema_erasure_properties.py::test_erasure_on_any_schema_never_bleeds_and_preserves_retained` · [property] `test_erase_subject_properties.py::test_erase_handles_every_reachable_row_and_never_bleeds` · [unit] `test_erase_subject.py::test_no_cross_subject_bleed` |
| **No cross-subject bleed — saga.** Outbox execution touches only the claimed entry's external record; completion fires only for the entry's own subject. | ADR 0010 | [property] `test_saga_runner_properties.py::test_executing_an_entry_n_times_converges_to_once`, `::test_every_subject_completes_exactly_once_and_only_for_itself` |
| **No cross-subject bleed — consent.** Status and history are scoped to one subject. | `consent/ledger.py` contract | [unit] `test_consent_ledger.py::test_no_cross_subject_bleed` |
| **Restriction derivation.** Status is latest-global OR latest-purpose with exact-timestamp ties resolving to the restricting record; a global restriction shadows any purpose-level lift; `status(purpose=None)` consults only the global records; no records means unrestricted; audit payloads carry the scope only, never `reason`/`source`; status and history are scoped to one subject. | ADR 0014; `restriction/ledger.py` contract | [property] `test_restriction_ledger_properties.py::test_status_equals_naive_rederivation`, `::test_audit_payloads_never_contain_reason_or_source` · [unit] `test_restriction_ledger.py::test_global_restriction_shadows_purpose_lift`, `::test_exact_timestamp_tie_resolves_to_restricted`, `::test_purpose_restriction_does_not_answer_global_status`, `::test_status_with_no_records_is_unrestricted`, `::test_payloads_never_contain_reason_or_source`, `::test_no_cross_subject_bleed` · [pg] `test_restriction_pg.py::test_restriction_flow_round_trips_on_postgres` |
| **Retention sweep is report-only and honest.** `RetentionSweeper.sweep()` never mutates any table; a duration without an anchor — and rows whose anchor is NULL — are counted indeterminate, never matched as expired; expired rows are attributed per subject through the hop chains with no cross-subject bleed; `RETENTION_EXPIRED` payloads carry table/column names and counts only, never values or anchor timestamps; the planner stays time-free — `plan()` output is unaffected by any `duration`/`anchor` value. | ADR 0012 | [property] `test_retention_sweeper_properties.py::test_sweep_never_mutates_any_table`, `::test_no_anchor_is_never_matched_as_expired`, `::test_plan_is_unaffected_by_duration_and_anchor` · [unit] `test_retention_sweeper.py::test_duration_without_anchor_is_wholly_indeterminate`, `::test_null_anchor_rows_count_as_indeterminate`, `::test_no_cross_subject_bleed_in_attribution`, `::test_event_payload_carries_names_and_counts_only`, `::test_repeated_sweep_reemits` |
| **Retained-category preservation.** A `RETAIN` column is never deleted or anonymized by any code path; its cells survive erasure byte-identically; retention requires a named legal reason. | gdpr-semantics rule ("retention is sacred"); ADR 0007 | [generated] `test_generated_schema_erasure_properties.py::test_erasure_on_any_schema_never_bleeds_and_preserves_retained` · [property] `test_erasure_planner_properties.py::test_retain_columns_never_in_delete_or_anonymize_steps`, `::test_row_deletion_requires_full_ownership_and_all_delete` · [unit] `test_erasure_planner.py::test_retained_child_under_deleted_subject_raises_retention_violation`, `test_erasure_executor.py::test_retain_counts_rows_without_touching_them`, `test_annotations.py::test_retain_without_policy_is_rejected` · [pg] `test_erase_subject_pg.py::test_committed_erasure_retains_invoice_and_reports_counts` |
| **Retention surfaces in exports.** Retained fields are still exported, carrying the declared retention reason, legal basis, and purpose. | Art. 15 semantics in `export/` | [generated] `test_generated_schema_export_properties.py::test_export_surfaces_retention_reasons_and_art15_metadata` · [unit] `test_exporter.py::test_retained_field_is_still_exported` |
| **No cross-subject bleed — rectification.** Rectifying subject A leaves every one of subject B's cells byte-identical, in every table, on any drawn schema. | ADR 0013 | [generated] `test_rectifier_properties.py::test_rectification_never_bleeds_across_subjects` · [unit] `test_rectifier.py::test_no_cross_subject_bleed` |
| **Rectification converges.** A second identical `rectify_subject` run changes nothing further and reports the same matched rows; externally, resolvers report `already_consistent=True` on re-application (the rectification analogue of `already_absent`). | ADR 0013 | [generated] `test_rectifier_properties.py::test_rectification_is_convergent` · [unit] `test_saga_runner.py::test_already_consistent_is_success` · conformance `test_conformance_suite.py` (`test_rectify_is_idempotent`, `test_rectify_of_absent_subject_is_consistent_success`) |
| **Strategy does not gate rectification.** `RETAIN` and `ANONYMIZE` columns of a corrected category are rewritten too — Art. 16 does not defer to Art. 17 annotations. | ADR 0013 | [unit] `test_rectifier.py::test_retain_columns_are_rectified_too`, `::test_anonymize_and_retain_strategies_do_not_gate_rectification` |
| **No terminal outbox row retains a payload.** A rectify entry's corrections payload (real PII) survives retries (`FAILED` keeps it) and is NULL the moment the entry is `SUCCEEDED` or `ABANDONED`. | ADR 0013 | [property] `test_saga_runner_properties.py::test_no_terminal_entry_retains_a_payload` · [unit] `test_outbox.py::test_succeeded_clears_the_payload`, `::test_abandoned_clears_the_payload`, `::test_failed_keeps_the_payload_for_the_retry` · [pg] `test_saga_runner_pg.py::test_corrections_payload_round_trips_jsonb_and_clears_on_success` |
| **Completion isolation per operation.** `ERASURE_COMPLETED` fires iff the subject's erase entries all succeeded, never counting rectify entries — and symmetrically for `RECTIFICATION_COMPLETED`; at most once per subject+operation. | ADR 0013 (amending ADR 0010) | [property] `test_saga_runner_properties.py::test_completion_isolation_per_operation` · [unit] `test_outbox.py::test_completion_is_scoped_per_subject_and_operation` · [pg] `test_saga_runner_pg.py::test_erase_and_rectify_complete_independently_under_real_locking` |
| **Rectification audit carries no values.** Old and new values never appear in any `RECTIFICATION_*` event payload — names and counts only. | ADR 0013; gdpr-semantics rule | [unit] `test_rectifier.py::test_no_audit_payload_ever_carries_a_corrected_value`, `::test_audit_sequence_and_payloads` · semgrep gate `.semgrep/audit-no-pii.yml` |
| **Manifest round-trip & migration.** Serialization never loses a declaration; old manifests migrate forward, never rejected; newer-than-known versions fail loudly. | gdpr-semantics rule; `manifest/migration.py` | [generated] `test_manifest_properties.py::test_collected_manifest_from_any_schema_round_trips` · [property] `test_manifest_properties.py::test_any_manifest_round_trips` · [unit] `test_manifest.py::test_payload_round_trip`, `::test_future_schema_version_is_rejected_loudly` |
| **Completeness findings exactly complement the data map.** Every table is either in the data map, whole-table flagged, or `effaced_*`-exempt; within a mapped table every column is annotated, a PK/FK, or flagged — nothing falls through, nothing is double-counted. Stale gate exemptions fail. | `lint/` + `adapters/sqlalchemy/completeness_linter.py` contract; docs `concepts/completeness` | [generated] `test_completeness_properties.py::test_findings_exactly_complement_the_data_map` · [unit] `test_completeness_linter.py` (whole suite, incl. `::test_stale_exemptions_fail_loudly`) |
| **Reachability findings are the exact inverse of resolution.** `lint_reachability(data_map, registry) == ()` if and only if `resolve_subject_graph(data_map, registry)` succeeds — every annotated store the planner cannot route back to a subject anchor (missing/extra anchor, missing subject path, unmapped table, M2M secondary, path not ending at subject, missing subject-id column, FK cycle) surfaces as a finding instead of a silently-unerased table. | issue #120; `lint/` + `adapters/sqlalchemy/reachability_linter.py` contract; docs `concepts/completeness` | [generated] `test_reachability_properties.py::test_findings_empty_iff_resolution_succeeds`, `::test_random_breakage_keeps_linter_and_resolution_aligned` (drawn structure-breaking mutations must fail both sides together) · [unit] `test_reachability_linter.py` (whole suite) · [unit] `test_cli_lint.py` (the `effaced lint` CLI exit-code surface over both linters) |
| **Erasure idempotent convergence.** Re-running `erase_subject` deletes nothing further and converges to the same state shape (anonymized cells legitimately get fresh surrogates, ADR 0009). | ADR 0009 | [generated] `test_generated_schema_erasure_properties.py::test_erasure_on_any_schema_is_idempotent` · [property] `test_erase_subject_properties.py::test_erase_handles_every_reachable_row_and_never_bleeds` (rerun phase) · [unit] `test_erase_subject.py::test_rerun_for_an_erased_subject_is_a_no_op_success` · [pg] `test_erase_subject_pg.py::test_rerun_for_an_erased_subject_is_a_no_op_success` |
| **Post-erasure verification is honest — verified ⇔ zero reachable residual rows in row-deleted tables; never claims more.** `ErasureVerifier.verify_subject_erased` re-reads the *same annotated surface* (SELECT counts only, no DML), `verified` is true iff every row-deleted table holds zero subject-scoped rows; surviving anonymize/retain counts are informational and never flip the verdict; a resurrected row-deleted row flips `verified` to false and names the table; one PII-free, scalar-only audit event per verification; un-annotated PII, off-path orphans, and anonymized cell *values* are out of scope by construction. | issue #121; ADR 0007/0009 | [generated] `test_generated_schema_verification_properties.py::test_erase_then_verify_is_verified_on_any_schema`, `::test_resurrecting_a_row_deleted_row_flips_verified` · [unit] `test_erasure_verifier.py` (whole suite: clean verdict, surviving never flips, resurrected row names the table, pre-erasure honesty, read-only, no cross-subject bleed, scalar-only payload) · [pg] `test_erasure_verifier_pg.py::test_verify_after_committed_erasure_is_clean_on_postgres` |
| **Saga idempotency & convergence.** Executing an entry N times equals executing it once (`entry_id` is the idempotency key; `already_absent=True` is success); any script of transient failures converges to SUCCEEDED or, past `max_attempts`, to a loudly audited ABANDONED. | ADR 0010; resolver idempotency contract | [property] `test_saga_runner_properties.py::test_executing_an_entry_n_times_converges_to_once`, `::test_any_transient_failure_script_converges` · [unit] `test_saga_runner.py::test_already_gone_is_success`, `::test_exhausted_retries_abandon_loudly` |
| **Exactly-once completion.** `ERASURE_COMPLETED` fires once per subject, only when every outbox entry for the subject is SUCCEEDED; an ABANDONED sibling blocks it forever. | ADR 0010 | [property] `test_saga_runner_properties.py::test_every_subject_completes_exactly_once_and_only_for_itself` · [unit] `test_saga_runner.py::test_completion_waits_for_the_subjects_last_entry`, `::test_an_abandoned_sibling_blocks_completion_forever`, `test_end_to_end_fault_injection.py::test_abandoned_sibling_blocks_completion_despite_a_succeeding_resolver` (via the planner's own enqueue) · [pg] `test_saga_runner_pg.py::test_concurrent_finishers_emit_exactly_one_completion`, `test_end_to_end_pg.py::test_full_pipeline_converges_to_completed_on_postgres` (whole pipeline, one wiring), `test_outbox_pg_survivors.py::test_mark_succeeded_serializes_against_a_held_sibling_lock` (the sibling FOR UPDATE serializes two concurrent finishers, so completion fires once) |
| **Claim is non-blocking and totally ordered under real Postgres.** `claim_batch` steps over a locked row (`SKIP LOCKED`) instead of waiting, and breaks ties on `enqueued_at` by `entry_id`; the operator read surfaces (`list_abandoned`, `list_scheduled`) break their respective ties the same way. These are `FOR UPDATE`/`SKIP LOCKED`/server-chosen-order contracts the SQLite mutation suite compiles away or cannot express, so their mutants survive the mutation run and stay on `mutation-equivalents.txt` by design — these tests are the evidence the run itself cannot produce. | ADR 0010; `saga/outbox.py` claim contract | [pg] `test_outbox_pg_survivors.py::test_claim_batch_skips_locked_rows_without_blocking`, `::test_claim_orders_tied_enqueued_at_by_entry_id`, `::test_list_abandoned_orders_tied_enqueued_at_by_entry_id`, `::test_list_scheduled_orders_tied_next_attempt_at_by_entry_id` · [unit] `test_claim_batch.py::test_an_entry_due_at_exactly_now_is_claimable` (the inclusive `<= now` boundary — killable without locking, so it lives in the SQLite suite, not the allowlist) |
| **Supervised requeue is erase-only, idempotent, and skip-tolerant.** `requeue(entry_ids)` flips only ABANDONED *erase* entries to PENDING with a fresh budget (`attempts=0`, `next_attempt_at=NULL`, `last_error=NULL`); missing or non-ABANDONED ids are silently skipped; calling it twice equals once; the return value reports only the entries that flipped, and a requeued entry re-enters the claimable population under its unchanged `entry_id`. | ADR 0015 | [unit] `test_outbox_requeue.py::test_requeue_flips_abandoned_to_pending_with_a_fresh_budget`, `::test_requeue_skips_missing_and_non_abandoned_ids`, `::test_requeue_is_idempotent`, `::test_requeued_entry_is_due_immediately_and_reclaimable`, `::test_requeue_resets_the_lease_gate_for_a_failed_then_abandoned_entry` · [pg] `test_saga_runner_pg.py::test_requeue_returns_an_abandoned_entry_to_pending_on_postgres`, `::test_requeue_serializes_against_a_held_row_lock` (FOR UPDATE lock order) |
| **Requeue refuses rectify entries — corrections are gone (ADR 0013 × 0015).** An ABANDONED rectify entry has had its corrections (PII) cleared at abandonment, so requeuing it would re-execute a no-op rectification that still completes (Art. 16 hole). `requeue` raises `ConfigurationError` naming the entry **before any append or flip** — including when mixed with valid erase ids, where nothing flips and no event is written; remediation is re-issuing via the `Rectifier`. | ADR 0013; ADR 0015 | [unit] `test_outbox_requeue.py::test_requeue_refuses_an_abandoned_rectify_entry`, `::test_requeue_of_a_mixed_batch_refuses_all_before_any_flip` |
| **Requeue audits append-first, PII-free.** Before any row flips, one `ERASURE_REQUEUED` event is appended per entry, carrying `{entry_id, resolver, prior_attempts, prior_error}` with the prior error as the exception class name only (omitted when absent); a down sink raises before any transition (nothing flips — all-or-nothing across the batch), and an outbox without an `audit_sink` refuses to requeue. | ADR 0015; gdpr-semantics rule | [unit] `test_outbox_requeue.py::test_requeue_appends_an_erasure_event_class_name_only`, `::test_requeue_append_first_no_flip_when_sink_is_down`, `::test_requeue_is_all_or_nothing_when_the_sink_dies_mid_batch`, `::test_requeue_without_an_audit_sink_raises_clearly` |
| **Backup-replay derivation is pure and honest.** `ReplayPlan.derive` is a pure function of the event set — any ordering yields an equal plan; a subject is replayable iff a qualifying `ERASURE_LOCAL_COMPLETED` exists at/after `backup_taken_at` (inclusive boundary), with the latest qualifying event cited as evidence; interrupted attempts are surfaced `indeterminate` and failed-only attempts `failed_only` — both never executed, never guessed; a naive cutoff raises `ConfigurationError` before anything else. | ADR 0023 | [property] `test_replay_properties.py::test_derivation_is_a_pure_function_of_the_event_set` · [unit] `test_replay_plan.py` (whole suite, incl. `::test_completion_at_exactly_the_backup_instant_is_replayed`, `::test_interrupted_attempt_is_indeterminate`, `::test_naive_backup_timestamp_raises_configuration_error`) · [unit] `test_replayer.py::test_indeterminate_subjects_are_never_executed` |
| **Replay converges and audits append-first.** `Replayer.replay` delegates to `erase_subject` (no second erasure engine): resurrected rows are erased again with no cross-subject bleed and RETAIN cells preserved; one `ERASURE_REPLAYED` event precedes each subject's re-run sequence, scalar-only payload, and a down sink stops the replay before any mutation; the first failure re-raises with later entries untouched; a replay of a replay is a no-op success; the default replay is local-only — external re-enqueueing requires `refs_for` and converges under the resolver idempotency contract. | ADR 0023 | [generated] `test_replay_properties.py::test_replay_on_any_schema_restores_the_post_erasure_state` · [unit] `test_replayer.py` (whole suite) · [pg] `test_replay_pg.py::test_replay_from_read_since_converges_on_postgres` |
| **`read_since` reads the whole window faithfully.** `DatabaseAuditSink.read_since(since)` returns every subject's events with `occurred_at >= since` (inclusive), oldest first with `event_id` tie-break, and fails all-or-nothing on an unreadable `event_type`; a timezone-naive `since` raises `ConfigurationError` (a naive bound on a timestamptz column silently shifts the window); the sink satisfies the `ReplaySource` capability protocol. | ADR 0023 | [unit] `test_database_audit_sink.py::test_read_since_returns_the_window_across_subjects_oldest_first`, `::test_read_since_breaks_occurred_at_ties_by_event_id`, `::test_read_since_unknown_event_type_raises_with_upgrade_guidance`, `::test_read_since_rejects_a_naive_bound`, `::test_database_sink_is_a_replay_source` · [pg] `test_replay_pg.py::test_replay_from_read_since_converges_on_postgres` |
| **A schedule is never a completion (retention-only erasure).** An erase entry routed to a `RetentionOnlyResolver` calls `schedule_erasure`, never `erase_subject`; a future horizon appends `ERASURE_EXPIRY_SCHEDULED` (append-first) and parks the entry `SCHEDULED` with the horizon as the claim gate and a fresh budget (`attempts=0`, `last_error=NULL`); a parked entry blocks `ERASURE_COMPLETED` until a post-horizon claim verifies `already_absent=True` (`ERASURE_STEP_SUCCEEDED` with `verified_expiry`, no `strategy` key — nothing was deleted by us); for any script of transients/horizons/absence the outbox converges (exhaustion = vendor purge), completion fires at most once and only when every entry SUCCEEDED, and every park is audited; exports of a scheduled subject stamp `expires_at` while local records carry `None`. | ADR 0022 | [property] `test_saga_runner_properties.py::test_completion_honesty_for_any_retention_only_script` · [unit] `test_retention_only_saga.py` (whole suite: park/gate/verify, slipped horizons, clamp, budget reset, mixed-subject blocking, taxonomy), `test_outbox.py::test_mark_scheduled_parks_with_the_horizon_and_a_fresh_budget`, `::test_scheduled_entry_is_not_claimable_before_its_horizon`, `::test_scheduled_entry_is_reclaimed_once_the_horizon_passes`, `test_exporter_resolvers.py::test_retention_only_records_carry_the_schedule_horizon` · conformance `test_conformance_suite.py::TestInMemoryRetentionOnlyResolverConformance` (+ erase tests skip / scheduled section gating meta-tests) · [pg] `test_saga_runner_pg.py::test_parked_entry_is_reclaimed_after_the_horizon_and_completes_once` (SKIP LOCKED re-claim path) |
| **Outbox status counts agree and are zero-filled.** SQL-side aggregation (`SqlStatusCountsSource`) returns the same per-status mapping as the Python-side count for any population, and both zero-fill every `OutboxStatus` member — the operator's `ABANDONED` signal is never a missing key. | issue #54; `saga/status_counts_source.py` contract | [property] `test_outbox_read_surface.py::test_sql_and_python_status_counts_agree` · [unit] `test_outbox_read_surface.py::test_status_counts_are_zero_filled`, `::test_status_counts_track_lifecycle_transitions` (both parameterized over Python/SQL paths) · [pg] `test_outbox_status_counts_pg.py::test_sql_counts_match_python_counts_under_real_postgres`, `::test_zero_filled_on_an_empty_outbox` |
| **FK-safe ordering.** Local steps follow the metadata-derived deletion order: children before parents, subject last; cycles rejected. | ADR 0007; `fk_safe_deletion_order` | [property] `test_resolution_properties.py::test_every_child_is_deleted_before_its_parent`, `test_erasure_planner_properties.py::test_local_targets_follow_deletion_order` · [generated] `test_schema_strategies.py::test_planner_row_deletion_matches_generator_expectation` (classification), `test_generated_schema_erasure_properties.py` (execution on drawn trees incl. multi-hop, deep self-referential chains, and composite FK hops) |
| **FK-based resolution matches ORM resolution.** `resolve_subject_graph_from_fk(data_map, metadata)` resolves subject-link paths from foreign-key constraints alone (no ORM mappers) and produces a graph identical in shape and guarantees to `resolve_subject_graph` — same hops, FK-safe order, and `fully_pii_owned` classification — so adapters without an ORM registry (reflected schemas, the Django adapter) drive the same engine. Missing/ambiguous FKs and unknown path tables fail loudly. | issue #62; `adapters/sqlalchemy/resolution.py` | [unit] `test_resolve_subject_graph_from_fk.py` (whole suite, incl. `::test_parity_with_orm_resolver`, `::test_missing_foreign_key_fails_loudly`) |
| **Django adapter reuses the engine without bleed.** The Django adapter translates `Model._meta` into effaced-annotated SQLAlchemy metadata and erases a subject through the unchanged executors, leaving every other subject's rows intact. | issue #62; `effaced-django` | [unit] `packages/effaced-django/tests/test_erase_subject_django.py::test_erase_one_subject_leaves_the_other_untouched`, `::test_subject_graph_is_fk_safe` |
| **Unsupported shapes fail loudly, never partially.** A row-deleted ancestor with a surviving child on the subject hop chain is rejected at plan time (`RetentionViolationError` for a retained child, `ManifestError` for one declaring nothing erasable), deterministically and before any DML — the seeded rows stay byte-identical after an attempted erase. A subject path through a many-to-many secondary table is refused by `resolve_subject_graph` (`SubjectResolutionError` naming the relationship), never a graph that silently omits the link table. | ADR 0007; gdpr-semantics rule ("retention is sacred", never-partial) | [generated] `test_rejected_schema_properties.py::test_conflicting_schema_is_rejected_before_any_dml`, `::test_many_to_many_subject_path_is_rejected_naming_the_link` · [unit] `test_erasure_planner.py::test_retained_child_under_deleted_subject_raises_retention_violation`, `test_resolution.py` (many-to-many rejection) |
| **Audit sequence & append-only.** Validation failures raise before any event; every outcome (success, failure, retention decision, abandonment) is audited; payloads carry references and exception class names, never rich PII; sinks have no update/delete surface. | ADR 0009/0010; gdpr-semantics rule | [unit] `test_erase_subject.py::test_audit_sequence_and_payloads`, `::test_unmatched_ref_kind_fails_loudly_before_any_event`, `test_exporter.py::test_malformed_subject_id_raises_before_any_audit_event`, `test_database_audit_sink.py::test_duplicate_event_id_is_rejected_never_overwritten`, `::test_read_unknown_event_type_raises_with_upgrade_guidance` · semgrep gate `.semgrep/audit-append-only.yml`, `.semgrep/audit-no-pii.yml` |
| **Ref→resolver routing.** A ref routes to the resolver whose `name` equals its `kind`; an unmatched kind raises before any audit event; a resolver with no matching ref is skipped and recorded, never an error. | ADR 0008 | [unit] `test_erase_subject.py::test_refs_route_to_the_resolver_named_by_their_kind`, `::test_unmatched_ref_kind_fails_loudly_before_any_event`, `::test_resolver_without_matching_ref_is_skipped_and_audited`, `test_exporter_resolvers.py::test_ref_without_resolver_raises_before_any_audit_event`, `::test_resolver_without_matching_ref_is_skipped_not_incomplete` |
| **Settings-driven registration is all-or-nothing per spec; skips are recorded, never silent.** A resolver registers iff every required key is present (non-blank); zero present is a recorded skip with the missing key names; a partial set raises `ConfigurationError` naming the missing keys (never a value); the builder receives exactly the declared present keys; a name mismatch raises. No auto-discovery — only declared specs register. | gdpr-semantics rule ("registration stays explicit"); issue #58 | [property] `test_registry_from_settings_properties.py::test_registered_iff_all_required_present`, `::test_nonempty_proper_subset_raises`, `::test_outcomes_align_with_specs` · [unit] `test_registry_from_settings.py` (whole suite, incl. `::test_configuration_error_never_leaks_a_value`, `::test_builder_receives_exactly_the_declared_keys`, `::test_blank_value_counts_as_absent`, `::test_name_mismatch_raises`, `::test_duplicate_names_across_specs_raise_resolver_error`) |
| **Export failures are never silent.** A failed resolver lands in `incomplete_sources`; local records survive the failure. | `export/` contract | [unit] `test_exporter_resolvers.py::test_failing_resolver_lands_in_incomplete_sources`, `::test_local_records_survive_resolver_failure` |
| **Atomic local phase.** Local row changes and outbox entries commit or roll back together, in the caller's transaction; audit evidence persists independently. | ADR 0009; gdpr-semantics rule | [unit] `test_erase_subject.py::test_rollback_discards_rows_and_outbox_but_keeps_audit`, `test_outbox.py::test_rollback_discards_enqueued_entries` · [pg] `test_erase_subject_pg.py::test_killed_transaction_rolls_back_rows_and_outbox_together`, `test_end_to_end_pg.py::test_full_pipeline_retains_invoice_and_never_touches_subject_two` (committed entries claimed by the saga on real locking) |
| **S3 resolver semantics** (paths relative to `packages/effaced-s3/tests/`). Erasure removes every version and delete marker under the prefix and only under it; nothing listed is success (`already_absent=True`); a partially failed batch raises `PartialEraseError` and retries converge; exports are page-size invariant; a blank or non-`/`-terminated prefix is refused before any S3 call (an unterminated prefix matches sibling subjects); messages and details never carry keys or prefixes. | `effaced-s3` README/CLAUDE.md; resolver idempotency contract (ADR 0010) | conformance: `test_s3_resolver_conformance.py` (shared `ResolverConformanceSuite`) · [property] `test_s3_resolver_properties.py::test_erase_converges_and_never_bleeds`, `::test_export_is_invariant_under_page_size`, `::test_partial_failure_retries_to_convergence` · [unit] `test_s3_resolver.py` (versioned/unversioned erase, batch bounds, taxonomy, prefix guard, leak checks) |
| **Supabase Storage resolver semantics** (paths relative to `packages/effaced-supabase/tests/`; rides `effaced-s3` machinery, ADR 0016). Erasure removes every current object under the prefix and only under it — and **never** calls `ListObjectVersions` (the gateway has no versioning, so current-object deletion is complete erasure); nothing listed is success (`already_absent=True`); a partially failed batch raises `PartialStorageEraseError` and retries converge; exports are page-size invariant and sourced `"supabase_storage"`; a blank or non-`/`-terminated prefix is refused before any gateway call; missing connection params raise `ConfigurationError`; messages and details never carry keys or prefixes. | `effaced-supabase` README/CLAUDE.md; ADR 0016; idempotency contract (ADR 0010) | conformance: `test_supabase_storage_conformance.py` (shared `ResolverConformanceSuite`) · [property] `test_supabase_storage_properties.py::test_erase_converges_and_never_bleeds`, `::test_export_is_invariant_under_page_size`, `::test_partial_failure_retries_to_convergence` · [unit] `test_supabase_storage_resolver.py` (current-object erase, no-versioning proof, taxonomy, prefix guard, construction guard, leak checks) |
| **Resend resolver semantics** (paths relative to `packages/effaced-resend/tests/`). Export maps `contact.email` (CONTACT), `contact.first_name`/`contact.last_name` (IDENTITY), and `contact.unsubscribed` (BEHAVIORAL); the caller-defined `properties` blob is never exported; erasure deletes the contact by email and a 404 on get/delete is success (`already_absent=True`); erasure touches exactly the target contact — path-hostile emails (`/`, `..`, `%`, dot-only values) are percent-encoded into a single path segment, never normalized into another endpoint; 4xx other than 404/429 raise `ResolverError` (never carrying the email or key), 429/5xx/connection faults propagate for saga retry. | issue #60; `effaced-resend` README/CLAUDE.md; idempotency contract (ADR 0010) | conformance: `test_resend_conformance.py::TestResendResolverConformance` (shared `ResolverConformanceSuite`) · [property] `test_resend_properties.py::test_erase_touches_exactly_the_target_contact` · [unit] `test_resend_resolver.py` (field mapping, properties exclusion, encoding guards, erase details), `test_resend_errors.py` (taxonomy matrix, leak guard) |
| **Each first-party resolver's claimed coverage is declared and conformance-tested (subset + enumeration + exclusion-absence).** Every first-party resolver attests a `CoveredSurface` (`AttestingResolver`) built from the same field tuples its exporter uses, so declaration and implementation cannot drift; the shared suite checks the surface names the resolver, every exported field of a present subject matches a declared `CoveredField` glob of the same category (subset), a fully-populated subject's export exercises every declared field (enumeration), and no exported field matches a `SurfaceExclusion` glob (absence). A covered surface declares *claimed* reach — never proof the external system holds no PII elsewhere, never a compliance determination. | issue #123; ADR 0008/0016; gdpr-semantics wording rule | conformance (shared `ResolverConformanceSuite`: `test_covered_surface_names_the_resolver`, `test_export_stays_within_the_declared_surface`, `test_declared_exclusions_never_appear_in_exports`, `test_fully_populated_export_enumerates_the_declared_surface`): `test_resolver_conformance.py` (stripe), `test_s3_resolver_conformance.py` (s3), `test_supabase_auth_conformance.py`, `test_supabase_storage_conformance.py`, `test_resend_conformance.py`, and `test_conformance_suite.py` (the suite's own meta-tests: skip-gating for non-attesting resolvers + the missing fully-populated hook, and positive subset/enumeration/exclusion assertions against the `InMemoryResolver` reference) |
| **Stripe rectify convergence** (Art. 16; paths relative to `packages/effaced-stripe/tests/`). `rectify_subject` maps corrections through the curated `IDENTITY → name`, `CONTACT → email` table and ignores every other category; only drifted fields are written; an absent customer (404) and a re-applied correction Stripe already reflects both report `already_consistent=True` (the analogue of `already_absent`); the modify path carries the same error taxonomy as erase (401 ⇒ `ResolverError`, 429 propagates) and never leaks the subject ref. | issue #96; ADR 0013; idempotency contract (ADR 0010) | conformance: `test_resolver_conformance.py::TestStripeResolverConformance` (`test_rectify_of_present_subject_succeeds`, `test_rectify_is_idempotent`, `test_rectify_of_absent_subject_is_consistent_success`, via `make_corrections`) · [unit] `test_stripe_rectify.py` (curated-map no-op, partial-drift writes only the drifted field, second-call convergence, absent success, taxonomy on modify, ref-leak guard) |
| **FastAPI layer is a thin veneer** (paths relative to `packages/effaced-fastapi/tests/`). The mounted endpoints execute the same engines under the same contracts — erasure via the router anonymizes/retains/never bleeds exactly as a direct `erase_subject` call, requests run in one transaction, validation failures reach no engine, refs flow into the outbox, and a `def` route resolves an `async` subject provider (the threading shape ADR 0020 depends on). | ADR 0020; ADR 0006 | [unit] `test_router_defaults.py` (trio, no-bleed, retained, 422-before-engine) · `test_external_refs.py::test_erase_enqueues_and_saga_converges` · `test_async_subject_provider.py::test_sync_routes_resolve_async_subject_providers` · `test_dependency_overrides.py` (session identity overrides) · `test_saga_worker.py` (drain, failure survival, lifespan) |
| **Alembic coexistence & additive backfill.** `bind_tables()` on the host's `target_metadata` is the whole integration: autogenerate against an empty database proposes exactly the four owned tables and their five indexes; after `create_all` a re-run is diff-free even with `compare_type`/`compare_server_default` enabled (no perpetual noise); an additive column carrying a server default backfills an already-populated table when applied — the `effaced_outbox.operation` precedent (ADR 0013), generalized. | ADR 0021; ADR 0013 | [pg] `test_alembic_autogenerate_pg.py::test_autogenerate_discovers_all_owned_tables`, `::test_owned_tables_autogenerate_clean_after_create_all`, `::test_additive_column_with_server_default_backfills_populated_outbox` · [unit] `test_bind_tables.py::test_index_names_are_stable_and_within_pg_limit`, `::test_no_server_defaults_except_the_outbox_operation_migration_aid` |

## Fault-injection matrix

Each cell injects one failure at one seam and asserts the resulting state
against ADR 0009/0010. End-to-end cells live in
`test_end_to_end_fault_injection.py` (erase_subject → outbox → saga runner
on one wiring); the same wiring's success path also runs on real Postgres
in `test_end_to_end_pg.py`, so the planner→claim handoff and the completion
lock are exercised under `FOR UPDATE` / `SKIP LOCKED`, not only on SQLite.

| # | Failure | Expected outcome | Proven by |
|---|---|---|---|
| 1 | Local executor step fails **before commit** | Caller rolls back: every table byte-identical, outbox empty, resolver never called, saga finds nothing; trail = REQUESTED · k×STEP_SUCCEEDED · STEP_FAILED | `test_end_to_end_fault_injection.py::test_failure_before_local_commit_leaves_no_partial_erasure` · `test_erase_subject.py::test_mid_stream_failure_is_audited_and_propagates` · [pg] `test_erase_subject_pg.py::test_partial_deletion_failure_rolls_back_and_audits` |
| 2 | Outbox enqueue fails **before commit** | STEP_FAILED with `target: "outbox"`, original exception re-raises | `test_erase_subject.py::test_enqueue_failure_is_audited_as_outbox_step` |
| 3 | Audit sink down **before commit** | Sink failure on REQUESTED stops the erasure before any row changes; a step whose outcome cannot be recorded audits as failed | `test_erase_subject.py::test_failing_sink_stops_the_erasure_before_any_row_changes`, `::test_transient_sink_failure_after_a_step_audits_it_as_failed` |
| 4 | Resolver fails **transiently after commit** | Entry FAILED with backoff (`min(base·2^(attempts−1), max)`), failure not audited (`last_error` on the row), converges to SUCCEEDED and exactly one ERASURE_COMPLETED | `test_end_to_end_fault_injection.py::test_transient_external_failure_after_commit_converges_to_completed` · `test_saga_runner.py::test_transient_failure_schedules_a_backoff_retry` · `test_saga_runner_properties.py::test_any_transient_failure_script_converges` |
| 5 | Resolver fails **terminally after commit** (`ResolverError` or retries exhausted) | Entry ABANDONED, STEP_FAILED with `abandoned: true`, ERASURE_COMPLETED never fires, abandoned entry never reclaimed — and the committed local erasure stays (no compensation; there is no un-erase) | `test_end_to_end_fault_injection.py::test_terminal_external_failure_after_commit_keeps_local_erasure` · `test_saga_runner.py::test_non_retryable_resolver_error_abandons_immediately`, `::test_exhausted_retries_abandon_loudly`, `::test_unknown_resolver_abandons_instead_of_wedging_the_queue` · multi-resolver subject (one succeeds, one abandons), driven through the planner: `test_end_to_end_fault_injection.py::test_abandoned_sibling_blocks_completion_despite_a_succeeding_resolver` |
| 6 | Runner **crashes between execute and bookkeeping** | Entry stays IN_FLIGHT; lease expiry re-claims; idempotency (`already_absent`) converges to the single-execution outcome; attempts count claims, so crash loops still converge to ABANDONED | `test_saga_runner_properties.py::test_executing_an_entry_n_times_converges_to_once` · `test_claim_batch.py::test_expired_lease_is_reclaimed` · [pg] `test_saga_runner_pg.py::test_expired_lease_lets_a_second_runner_take_over` |
| 7 | Audit sink down **while the runner books an outcome** | Append precedes the status change: the entry stays IN_FLIGHT, no outcome is recorded without its audit record; after the sink heals and the lease expires, the retry converges and both success and completion are recorded | `test_end_to_end_fault_injection.py::test_sink_outage_during_settle_keeps_entry_in_flight_and_heals` |
| 8 | **Two runners race** for the same entries / the same subject's last entries | `FOR UPDATE SKIP LOCKED` claims each entry exactly once; completion check locks the subject's rows in entry-id order, exactly one runner observes the transition | [pg] `test_saga_runner_pg.py::test_two_concurrent_runners_process_each_entry_exactly_once`, `::test_concurrent_finishers_emit_exactly_one_completion`, `::test_skip_locked_skips_rows_held_by_an_open_claim` |

## Coverage gate

`just cov` and CI's test job run pytest with `--cov`; the workspace
`pyproject.toml` sets `[tool.coverage.report] fail_under = 95` (branch
coverage, sources `effaced` + `effaced_stripe` + `effaced_supabase` + `effaced_s3` + `effaced_resend`), so any run below 95% fails
the build. Issue #18 asked for a gate of at least 85% on
`packages/effaced/src` — the enforced gate exceeds it.

## How deep the properties run

Hypothesis profiles (workspace `conftest.py`): dev default 100 examples,
`--hypothesis-profile=ci` 300 (every PR), `--hypothesis-profile=deep` 2500
(weekly `deep-checks.yml`). Schema-per-example tests scale via
`schema_strategies.scaled_examples(n)` — a fraction of the profile budget,
because each example builds metadata, ORM mappers, and an in-memory
database. Mutation testing (mutmut, weekly) backstops assertions the line
coverage cannot see — and over `audit/consent/erasure/manifest/saga` it is a
**hard gate** (#124): every surviving mutant must either be killed by a test
or be a named equivalent in `packages/effaced/mutation-equivalents.txt`, so
mutation survival on those modules is CI-enforced down to that documented
equivalent floor.

## SQLite vs Postgres caveat

Unit and property tests run on in-memory SQLite, which silently drops
`FOR UPDATE` / `SKIP LOCKED`. Every locking or concurrency guarantee
(matrix row 8, exactly-once completion under race) is therefore proven only
by the `[pg]` integration suite, which CI runs against a real Postgres on
every PR. The end-to-end wiring is no exception: while the fault matrix
exercises it on SQLite for breadth, its success path also runs on Postgres
(`test_end_to_end_pg.py`) so the full erase→outbox→saga handoff and the
completion lock are proven under real locking, not just the saga layer in
isolation. The same reason makes ten saga `Outbox` mutants permanently
survive the weekly SQLite mutation run: `test_outbox_pg_survivors.py` pins
their tied-ordering (`claim_batch`/`list_abandoned`/`list_scheduled`) and
lock-order (`SKIP LOCKED`, the `mark_succeeded` sibling lock) contract on
real Postgres — a contract the mutated SQLite suite cannot kill (a *dropped*
tie-break leaves no total order, so its mutant is not deterministically
killable anywhere; the test pins the contract the tie-broken query upholds).
Those mutants stay on `mutation-equivalents.txt` by design (listed as "not
killable by the mutated suite", not as equivalents; removing one fails the
gate as a stale entry). The inclusive `<= now` claim boundary is **not** among
them: it needs no locking, so it is killed in the SQLite suite
(`test_claim_batch.py::test_an_entry_due_at_exactly_now_is_claimable`).

## Keeping this file honest

Renaming, moving, or deleting a test listed here updates this file in the
same PR — stale guidance is a bug (see `.claude/rules/testing.md`). New
guarantees land with their row added here.
